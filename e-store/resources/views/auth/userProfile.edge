@include('layouts/main') 

<div class="container mx-auto mt-10 px-4">
  <div class="flex flex-col lg:flex-row space-y-4 lg:space-y-0 lg:space-x-6">
    <!-- Menu Profile -->
    <div class="w-full lg:max-w-sm bg-white p-4 rounded shadow-md flex justify-center items-center">
			@include('components/menuProfile')
		</div>		

    <!-- Formulário de Perfil -->
    <div class="w-full lg:w-2/3 bg-white p-6 rounded shadow-md">
      <h1 class="text-2xl font-bold mb-6">Perfil do Usuário</h1>
      
      <form method="POST" action="{{ route('auth.register', { id: auth.user?.id }) }}">
		<input type="hidden" name="_method" value="{{ auth.user?.id ? 'PUT' : 'POST' }}">
		<input type="hidden" name="id" value="{{ auth.user?.id }}">
	  
		<div class="mb-4">
		  <label for="full_name" class="block text-sm font-medium text-gray-700">Nome Completo</label>
		  <input 
			type="text" 
			id="full_name" 
			name="full_name" 
			value="{{ auth.user?.full_name }}" 
			class="mt-1 block w-full border border-gray-300 rounded p-2" 
			required>
		</div>
	  
		<div class="mb-4">
		  <label for="email" class="block text-sm font-medium text-gray-700">E-mail</label>
		  <input 
			type="email" 
			id="email" 
			name="email" 
			value="{{ auth.user?.email }}" 
			class="mt-1 block w-full border border-gray-300 rounded p-2" 
			required>
		</div>
	  
		<div class="mb-4">
		  <label for="phone" class="block text-sm font-medium text-gray-700">Telefone</label>
		  <input 
			type="text" 
			id="phone" 
			name="phone" 
			value="{{ auth.user?.phone }}" 
			class="mt-1 block w-full border border-gray-300 rounded p-2">
		</div>
	  
		<div class="flex justify-end">
		  <button 
			type="submit" 
			class="bg-blue-500 text-white font-bold py-2 px-4 rounded hover:bg-blue-600 transition">
			Salvar Alterações
		  </button>
		</div>
	  </form>
	  
    </div>
  </div>
</div>
