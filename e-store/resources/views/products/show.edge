@include('layouts/main')
<div id="notification-container" style="position: fixed; top: 10px; right: 10px; z-index: 1000;"></div>
<div id="webcrumbs" class="flex justify-center py-10 bg-neutral-100">
    <div class="w-[1000px] p-6 bg-white shadow-lg rounded-lg flex gap-8">
        <!-- Left - Product Image -->
        <div class="w-full">
            <img src="{{ product.imageUrl }}" alt="{{ product.name }}"
                class="object-cover rounded-lg w-[400px] h-[400px] border border-neutral-200" />
            <div class="mt-4 flex gap-4">
                <button class="p-2 bg-black text-white rounded-md hover:bg-neutral-800">
                    <span class="material-symbols-outlined">favorite</span> Lista de Desejos
                </button>
                <button class="p-2 bg-black text-white rounded-md hover:bg-neutral-800">
                    <i class="fab fa-facebook"></i> Compartilhar
                </button>
            </div>
        </div>

        <!-- Right - Product Info -->
        <div class="w-[550px]">

            <!-- Title -->
            <h1 class="text-2xl font-title font-bold text-black mb-2">
                {{ product.name }}
            </h1>

            <!-- Product Code -->
            <div class="text-sm text-neutral-400 mb-4">
                Código: <span class="text-black">{{ product.id }}</span>
                <!-- Exibição da Categoria -->
                <span class="ml-4 text-neutral-400">
                    Categoria: <span class="text-black">{{ product.category.name }}</span>
                </span>
            </div>

            <hr class="my-4 border-neutral-300" />

            <!-- Price -->
            <div class="text-3xl font-bold text-black mb-1">R$ {{product.price}}</div>

            <!-- Quantity and Button -->
            

            <div class="text-sm mt-2">
                Estoque:
                @if (product.quantidade > 0)
                    <span class="text-green-600">Disponível</span>
                @else
                    <span class="text-red-600">Indisponível</span>
                @endif
            </div>
            <div class="flex items-center gap-4 mt-6">
                @if (product.quantidade > 0)
                <button class="h-[40px] w-[200px] bg-black text-white font-bold rounded-md hover:bg-neutral-800">
                    COMPRAR
                </button>
                @else
                <button class="h-[40px] w-[200px] bg-neutral-700 text-white font-bold rounded-md" disabled>
                    Indisponível
                </button>
                @endif
                
            </div>
            

            <hr class="my-4 border-neutral-300" />

            <!-- Payment Methods -->
            <div class="text-neutral-700 text-sm mb-4">
                @if (product.quantidade > 0)
                <div class="flex justify-between items-center mt-2">
                    <span>
                        <i class="fa-solid fa-truck"></i> Pagar na Entrega
                    </span>
                    <span>R$ {{product.price}}</span>
                </div>
                <div class="flex justify-between items-center mt-2">
                    <span>
                        <i class="fa-brands fa-paypal"></i> PagSeguro
                    </span>
                    <span>R$ {{product.price-(product.price*0.05)}}</span> <!-- 5% de desconto -->
                </div>
                <div class="flex justify-between items-center mt-2">
                    <span>
                        <i class="fa-solid fa-money-bill"></i> Boleto Bancário
                    </span>
                    <span>R$ {{product.price-(product.price*0.1)}}</span> <!-- 10% de desconto -->
                </div>
                @else
                <div class="flex justify-between items-center mt-2">
                    <span>
                        <i class="fa-solid fa-truck"></i> Pagar na Entrega
                    </span>
                    <span class="text-black">-</span>
                </div>
                <div class="flex justify-between items-center mt-2">
                    <span>
                        <i class="fa-brands fa-paypal"></i> PagSeguro
                    </span>
                    <span class="text-black">-</span>
                </div>
                <div class="flex justify-between items-center mt-2">
                    <span>
                        <i class="fa-solid fa-money-bill"></i> Boleto Bancário
                    </span>
                    <span class="text-black">-</span>
                </div>
                @endif
                
            </div>

            <hr class="my-4 border-neutral-300" />

            <!-- Shipping Calculator -->
            <div>
                @if (product.quantidade > 0)
                <label class="block text-neutral-700 text-sm mb-2">
                    CALCULE O FRETE
                </label>
                <div class="flex gap-4">
                    <input id="cep" type="text" placeholder="CEP"
                        class="w-[200px] h-[40px] p-2 border border-neutral-300 rounded-md text-black" />
                    <button id="calcular-frete" class="h-[40px] w-[60px] bg-black text-white rounded-md hover:bg-neutral-800">
                        OK
                    </button>
                </div>
                <div id="endereco" class="mt-4 text-neutral-700 text-sm">
                    <!-- Aqui será exibido o endereço -->
                </div>
                @else
                    
                @endif
                
            </div>
        </div>
    </div>
    @vite(['resources/js/cartSide.js'])
    @vite(['resources/js/navbar.js'])
    @vite(['resources/js/cart.js'])
</div>
<script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/js/all.min.js"></script>
<script>
    document.getElementById('calcular-frete').addEventListener('click', async function () {
        const cep = document.getElementById('cep').value;
        if (cep.length !== 8) {
            alert('Por favor, insira um CEP válido com 8 dígitos.');
            return;
        }

        try {
            const response = await fetch('/calculate-shipping', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify({ cep })
            });

            const data = await response.json();
            if (data.success) {
                document.getElementById('endereco').textContent = `Endereço: ${data.endereco.street}, ${data.endereco.neighborhood} - ${data.endereco.city}/${data.endereco.state}`;
            } else {
                document.getElementById('endereco').textContent = 'Erro ao buscar endereço. Tente novamente.';
            }
        } catch (error) {
            document.getElementById('endereco').textContent = 'Erro ao buscar endereço. Tente novamente.';
        }
    });
</script>
